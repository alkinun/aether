run_id = "deepseek-v3-dense-250m-1b"
run_state = "WaitingForMembers"

[config]
warmup_time = 240
cooldown_time = 5
epoch_time = 360
max_round_train_time = 60
round_witness_time = 5
min_clients = 1
init_min_clients = 1

# 62,500 * 32 * 512 = 1,024,000,000 tokens
global_batch_size_start = 32
global_batch_size_end = 32
global_batch_size_warmup_tokens = 0

verification_percent = 0
witness_nodes = 1
total_steps = 62500
waiting_for_members_extra_time = 10

[model.LLM]
architecture = "HfDeepseek"
data_type = "Pretraining"
max_seq_len = 512
cold_start_warmup_steps = 0

# Server data mode: the coordinator also hosts a DataProviderTcpServer (see the
# companion data.toml, passed via --data-config). Volunteers fetch only their
# assigned shard per round instead of each needing a local dataset copy. Because
# the coordinator hands each sequence to exactly one client once, total egress
# over the whole run is ~3.8 GB, not the full 20 GB on disk.
#
# The host MUST be the publicly reachable address volunteers connect to, and the
# port is both what they connect on and what the server binds (0.0.0.0:<port>).
# Expose this port (39406) in your firewall, and update the host if your server
# isn't reachable at this domain.
[model.LLM.data_location]
Server = "train.aethercompute.org:39406"

[model.LLM.checkpoint.Hub]
repo_id = "aethercompute/deepseek-v3-dense-250m-1b"

[model.LLM.lr_schedule.Cosine]
base_lr = 3.0e-4
warmup_steps = 1500
warmup_init_lr = 0.0
total_steps = 62500
final_lr = 3.0e-5

[model.LLM.optimizer.Distro]
clip_grad_norm = 1.0
compression_decay = 0.999
compression_chunk = 64
compression_topk = 8
quantize_1bit = true
weight_decay = 0.1
